rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read on programs
    match /programs_empty/{document=**} {
      allow read: if true;
      allow create: if request.auth != null && exists(/databases/$(database)/documents/volunteers/$(request.auth.uid));
      allow update: if request.auth != null && exists(/databases/$(database)/documents/volunteers/$(request.auth.uid));
      allow delete: if request.auth != null && exists(/databases/$(database)/documents/volunteers/$(request.auth.uid));
    }

    // Volunteers collection
    match /volunteers/{userId} {
      allow read, write: if request.auth.uid == userId || request.auth.uid != null;
    }

    // Admin state
    match /admin/{document=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }

    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
