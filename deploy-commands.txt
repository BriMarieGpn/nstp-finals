# Firebase Deployment Commands
# Copy and paste these commands to deploy your GrowSauYOU app

# 1. Make sure you're in the project directory
cd "c:\Users\rhejie carl\Downloads\nstp-finals-20260427T040545Z-3-001\nstp-finals"

# 2. Deploy to Firebase (hosting + firestore rules)
firebase deploy

# Alternative: Deploy only hosting
# firebase deploy --only hosting

# Alternative: Deploy only firestore rules
# firebase deploy --only firestore:rules